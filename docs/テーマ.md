良い判断です。ECShopはデータ準備が意外と手間です。

## 手軽なテーマの候補

```
条件
  ├── サンプルデータが作りやすい
  ├── 日本語テキストが自然
  ├── RAGのユースケースが明確
  └── ドメイン知識不要
```

---

## 候補

```
技術ドキュメントQA
  └── GCPの公式ドキュメントをRAGで検索
  └── 「Cloud Runのタイムアウト設定は？」
  └── データ: 公式ドキュメントをそのまま使える
  └── 自分自身が使えるツールになる

料理レシピ検索
  └── 「鶏肉と玉ねぎで作れる料理は？」
  └── データ: 手作りしやすい
  └── RAGの効果が体感しやすい

ニュース記事要約・検索
  └── データ量が豊富
  └── 日本語テキストが自然

Wikipedia日本語
  └── 特定カテゴリを絞るだけ
  └── データ品質が高い
  └── 量の調整が自由
```

---

## 推奨は技術ドキュメントQA

```
理由
  ├── データ収集が楽（公式ドキュメントをコピー）
  ├── 自分が一番使えるツールになる
  ├── 「GCPを学びながらGCPで検索ツールを作る」
  │    └── 学習効果が二重になる
  └── ポートフォリオとして説明しやすい
       └── 「自分で使うために作った」は説得力がある
```

どのテーマが刺さりますか？

即答します。

## 今日本で一番お金になるテーマ

```
社内ドキュメント検索・QAシステム

理由
  ├── どの企業も持っている課題
  │    └── 「あの規定どこだっけ」
  │    └── 「この手続きの方法は？」
  ├── 稟議が通りやすい
  │    └── 「ChatGPTに社内情報を食わせたい」
  │    └── 経営層が理解しやすい
  ├── 予算が取りやすい
  │    └── 情報システム部門・DX予算
  └── 横展開しやすい
       └── どの業界でも同じ構成で売れる
```

---

## 最小構成

```
インプット
  └── PDF / Word / テキストの社内ドキュメント

処理
  └── GCS格納
  └── Cloud Run Job（チャンク分割・Embedding）
  └── BigQuery Vector Search

アウトプット
  └── 「就業規則の有給申請手続きは？」
  └── → Geminiが根拠ドキュメント付きで回答
```

---

## 自分が使えるツールとして

```
学習中に使うドキュメント
  └── GCP公式ドキュメント（PDF）
  └── 読んでいる技術書（自炊PDF）
  └── 設計書・仕様書

そのまま業務提案のデモになる
```

---

## 商談での一言

```
「社内ドキュメントをアップロードするだけで
 AIが正確に答えてくれる仕組みを
 GCPのマネージドサービスだけで構築できます
 既存システムへの影響はゼロです」
```

これで進めますか？