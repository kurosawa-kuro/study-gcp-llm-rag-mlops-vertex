# 実装済み一覧

最終更新: 2026-04-04

---

## ディレクトリ構成

```
study-gcp-llm-rag-mlops-vertex/
├── .github/workflows/      # CI/CD（GitHub Actions）
├── data/sample/            # サンプル社内規定ドキュメント
├── docs/                   # 仕様・設計書
│   └── elastic-search/     # Elastic Search 仕様・設計書
├── makefiles/              # Makefile分割モジュール
├── scripts/                # 自動化スクリプト
├── src/
│   ├── doc-qa/
│   │   ├── ingestion/      # Cloud Run Job（ドキュメント取込）
│   │   └── api/            # Cloud Run Service（QA API）
│   └── elastic-search/     # Elastic Cloud基盤
└── terraform/              # GCPインフラ定義
```

---

## ファイル一覧

### ルート

| ファイル | 説明 |
|---------|------|
| `.env.example` | 環境変数テンプレート（GCS・BQ・ES・Discord） |
| `.gitignore` | 除外設定（credentials, tfstate等） |
| `CLAUDE.md` | Claude Code向けプロジェクト説明書 |
| `Makefile` | ビルド・デプロイコマンド統合エントリポイント |
| `README.md` | プロジェクト概要 |

### .github/workflows/

| ファイル | 説明 |
|---------|------|
| `doc-qa.yml` | Ingestion + API CI/CD（test→build→push→deploy） |
| `terraform.yml` | Terraform CI/CD（PR→plan表示、main→apply自動実行） |

### data/sample/

| ファイル | 説明 |
|---------|------|
| `就業規則_sample.txt` | サンプル就業規則（有給休暇・勤務時間・リモートワーク等） |
| `経費精算規定_sample.txt` | サンプル経費精算規定（交通費・出張旅費・会議費等） |
| `FAQ_sample.txt` | サンプルFAQ（15問） |

### docs/

| ファイル | 説明 |
|---------|------|
| `仕様、設計書.md` | 社内ドキュメントQAシステム 仕様書・基礎設計書 |
| `移行ロードマップ.md` | 住宅予測→LLM RAG 移行ロードマップ |
| `実装済み一覧.md` | 本ファイル |
| `elastic-search/仕様、設計書.md` | Elastic Search 仕様・設計書 |

### makefiles/

| ファイル | 説明 |
|---------|------|
| `ingestion.mk` | Ingestionのtest/build/deploy/runコマンド |
| `doc-qa-api.mk` | QA APIのtest/build/deploy/runコマンド |
| `gcp.mk` | GCP API有効化・SA権限・Docker認証設定 |
| `terraform.mk` | Terraform init/plan/apply/destroy/fmt |

### scripts/

| ファイル | 説明 |
|---------|------|
| `core.py` | 共通ユーティリティ（定数・JSON構造化ログ・load_env・run・dispatch・notify_discord） |
| `monitor_ingestion.py` | Ingestion Job実行結果監視 + Discord通知 |
| `monitor_qa_api.py` | QA API健全性チェック + Discord通知 |
| `upload_doc.sh` | GCSへのドキュメントアップロード |
| `query.sh` | QA APIへのクエリテスト |
| `setup_gcp.sh` | Google Cloud CLIインストール・初期設定 |
| `setup_terraform.sh` | Terraformインストール・初期設定 |
| `es_config.py` | Elastic Cloud 接続確認用の共通設定 |
| `es_docker_ops.py` | Elastic Cloud 接続確認用 Docker操作（build/push/clean） |
| `es_gcp_ops.py` | Elastic Cloud 接続確認用 GCP操作（execute/logs） |
| `es_tf_ops.py` | Elastic Cloud 関連 Terraform操作（統合済み terraform/ を参照） |

### src/doc-qa/ingestion/（ドキュメント取込パイプライン）

| ファイル | 説明 |
|---------|------|
| `main.py` | オーケストレーション（GCS取得→抽出→分割→Embedding→BQ/ES格納） |
| `extractor.py` | テキスト抽出（PDF: pymupdf / Word: python-docx / TXT） |
| `chunker.py` | チャンク分割（800文字・50文字オーバーラップ） |
| `embedder.py` | Vertex AI Embedding API呼び出し（バッチ100件/リクエスト） |
| `bq_writer.py` | BigQuery documents テーブル書き込み（冪等・リトライ付き） |
| `es_writer.py` | Elasticsearch kuromoji インデックス登録（冪等） |
| `Dockerfile` | Python 3.11-slim コンテナ定義 |
| `requirements.txt` | 依存パッケージ |
| `requirements-dev.txt` | 開発依存（pytest, pytest-mock） |
| `test_main.py` | 単体テスト（extractor, chunker, bq_writer） |

### src/doc-qa/api/（QA API）

| ファイル | 説明 |
|---------|------|
| `main.py` | FastAPIエントリポイント（/query, /ingest, /health） |
| `embedder.py` | クエリのVertex AI Embedding変換 |
| `retriever.py` | ハイブリッド検索（BigQuery Vector Search + Elasticsearch kuromoji） |
| `reranker.py` | RRF（Reciprocal Rank Fusion）リランク |
| `generator.py` | Vertex AI Gemini回答生成（プロンプト組み立て） |
| `Dockerfile` | Python 3.11-slim + FastAPI コンテナ定義 |
| `requirements.txt` | 依存パッケージ |
| `requirements-dev.txt` | 開発依存（pytest, httpx, pytest-mock） |
| `test_main.py` | 単体テスト（reranker, generator） |

### terraform/（モジュール分離構成）

| ファイル | 説明 |
|---------|------|
| `provider.tf` | Google + Elastic Cloud provider設定 |
| `variables.tf` | ルート変数（プロジェクト・リージョン・各種名前・ES認証情報） |
| `main.tf` | モジュール呼び出し（依存グラフ: registry→data→elastic→iam→compute） |
| `outputs.tf` | ルート出力（API URL・ESエンドポイント・SA email） |
| `modules/data/` | データ層: GCS バケット + BigQuery dataset/table |
| `modules/compute/` | コンピュート層: Cloud Run Job/Service + Scheduler + Job invoker IAM |
| `modules/elastic/` | 外部SaaS: Elastic Cloud デプロイメント + Secret Manager |
| `modules/registry/` | コンテナレジストリ: Artifact Registry |
| `modules/iam/` | 認証認可: SA + BQ/GCS/VertexAI/SecretManager 権限バインディング |
