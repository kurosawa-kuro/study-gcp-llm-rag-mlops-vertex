# 住宅予測 → 社内規定LLM RAG 移行ロードマップ

最終更新: 2026-04-04
ステータス: **全Phase実装・デプロイ・動作検証済み**

---

## Context

California Housing予測のMLOpsパイプラインを完全に廃止し、`docs/仕様、設計書.md`に定義された**社内ドキュメント検索・QAシステム**（LLM RAG）に置き換えた。移行後、Terraform モジュール分離・設定一元化・ディレクトリ責務分離のリファクタリングを実施し、GCPへのデプロイ・エンドツーエンド動作検証を完了。

---

## Phase 0: 削除（住宅予測コードの撤去）--- 実装済み

削除対象 ~30ファイル:
- `src/batch/`, `src/api/`, `src/pipeline/`, `notebooks/`
- 旧 CI/CD、旧 makefiles、旧 scripts、旧 docs

---

## Phase 1: インフラ構築 --- 実装・検証済み

Terraform モジュール分離構成で GCP + Elastic Cloud の全リソースを管理:

```
terraform/
├── provider.tf          # Google + Elastic Cloud（credentials.yml を yamldecode）
├── variables.tf         # ルート変数（elastic_cloud_api_key + google_ai_studio_api_key）
├── main.tf              # モジュールオーケストレーション
├── outputs.tf           # API URL・ESエンドポイント・SA email
├── terraform.tfvars     # シークレット変数（git管理外）
└── modules/
    ├── data/            # GCS + BigQuery dataset/table
    ├── compute/         # Cloud Run Job/Service + Scheduler + GOOGLE_AI_STUDIO_API_KEY
    ├── elastic/         # Elastic Cloud + Secret Manager（username/password 自動取得）
    ├── registry/        # Artifact Registry
    └── iam/             # SA + 最小権限バインディング
```

---

## Phase 2: Ingestion実装 --- 実装・検証済み

```
src/ingestion/
├── main.py              # オーケストレーション
├── extract/
│   ├── extractor.py     # テキスト抽出（PDF / Word / TXT）
│   └── chunker.py       # チャンク分割
├── embed/
│   └── embedder.py      # Vertex AI Embedding API
├── store/
│   ├── bq_writer.py     # BigQuery 冪等書き込み（ストリーミングバッファ対応）
│   └── es_writer.py     # Elasticsearch 登録（standard アナライザー）
├── tests/
│   ├── test_main.py     # extractor, chunker, bq_writer テスト
│   └── test_es_writer.py # es_writer テスト
└── Dockerfile           # build context=ルート
```

検証結果: 3ファイル取込→6チャンク→BQ+ES格納 成功

---

## Phase 3: QA API実装 --- 実装・検証済み

```
src/api/
├── endpoints/
│   └── main.py          # FastAPI（/query, /ingest, /health）
├── search/
│   ├── embedder.py      # クエリ Embedding
│   ├── retriever.py     # ハイブリッド検索（BQ Vector + ES）
│   └── reranker.py      # RRF リランク
├── generation/
│   └── generator.py     # Google AI Studio Gemini（gemini-2.5-flash）
├── tests/
│   ├── test_main.py     # reranker, generator テスト
│   └── test_retriever.py # retriever テスト
└── Dockerfile           # uvicorn endpoints.main:app
```

検証結果: 「有給休暇の申請手続き」→根拠ドキュメント付き日本語回答 成功

### LLM回答生成の技術選定

| 方式 | 状態 | 備考 |
|------|------|------|
| Vertex AI Gemini | 断念 | コンソールで利用規約同意が必要。`gcloud`/Terraform だけでは有効化不可 |
| Google AI Studio API | **採用** | `google-generativeai` SDK + APIキー。即時利用可能。`gemini-2.5-flash` |

将来 Vertex AI Gemini に戻す場合は `generator.py` のみ変更。

---

## Phase 4: CI/CD・スクリプト・ドキュメント --- 実装済み

### CI/CD
- `.github/workflows/doc-qa.yml` --- test→build→push→deploy
- `.github/workflows/terraform.yml` --- TF_VAR_ で `elastic_cloud_api_key` / `google_ai_studio_api_key` 注入

### スクリプト（サブフォルダ分離）
```
scripts/
├── monitor/ingestion.py       # Ingestion Job 監視
├── monitor/qa_api.py          # QA API 健全性チェック
├── ops/upload_doc.py          # GCS アップロード
├── ops/query.py               # QA API クエリ送信
├── setup/gcp.py               # GCP CLI インストール（スキップ付き）
├── setup/terraform.py         # Terraform インストール（スキップ付き）
├── setup/gcp_setup.sh         # GCP API有効化 + SA権限 + Docker認証（Makefileから呼出）
├── setup/bq_vector_index.sh   # BQ Vector Index 冪等作成（Makefileから呼出）
└── setup/tf_import_registry.sh # Artifact Registry Terraform import（Makefileから呼出）
```

---

## Phase 5: リファクタリング --- 実装済み

| 施策 | 内容 |
|------|------|
| 設定一元化 | `shared/config.py` を唯一の設定ローダーに。`env/config/application.yml` が Single Source of Truth |
| スクリプト基盤 | `shared/core.py` に gcloud / run / notify_discord を集約 |
| Terraform モジュール分離 | 5モジュール（data/compute/elastic/registry/iam） |
| Terraform 依存制御 | `module.compute` に `depends_on = [module.elastic]` で Elastic Cloud 完了後に Cloud Run を作成 |
| クレデンシャル統合 | `env/secret/credentials.yml`（GCP SA + Elastic Cloud + Gemini APIキー） |
| Dockerfile 修正 | build context をルートに。`__init__.py` を全サブフォルダに追加 |
| Makefile スクリプト化 | インラインシェルを `scripts/setup/*.sh` に分離。Makefile は薄いラッパーに徹する |
| bootstrap フェーズ分離 | `images-push`（全イメージ束ね）で漏れ防止、順序を Phase 0〜7 で明示 |
| ES 接続リトライ | API の `_get_es_client()` に最大3分のリトライ（Secret未作成・ES起動途中に対応） |
| BQ Vector Index 自動化 | `bq-vector-index` ターゲットで冪等作成（5000行未満はスキップ） |
| ES PoC 削除 | `src/elastic-search/` を完全削除（doc-qa に統合済み） |
| サブフォルダ分離 | scripts/ → monitor/ops/setup、ingestion/ → extract/embed/store、api/ → endpoints/search/generation |

---

## Phase 6: デプロイ・動作検証 --- 完了

### 検証結果

```
make destroy-all             → 全リソース削除成功（21リソース）
make deploy-all              → ゼロから全自動構築成功（20リソース作成）
curl /health                 → {"status":"ok","version":"1.0.0"}
query.py "有給休暇の申請手続き" → 根拠付き日本語回答 成功
query.py "出張の宿泊費上限は？" → 「1泊10,000円。東京23区・大阪市内は12,000円」
make test                    → 69テスト ALL PASSED
make eval                    → 20件完走（Recall@3=1.0, MRR=0.9, EM=0.65）
```

`destroy-all` → `deploy-all` のクリーンサイクルをゼロから検証済み。

### 検証時に修正した問題

| 問題 | 修正 |
|------|------|
| `__init__.py` 未作成 | 全サブフォルダに追加 |
| BQ ストリーミングバッファ DELETE 不可 | エラー時スキップに変更 |
| ES kuromoji 未インストール | standard アナライザーに変更 |
| Vertex AI Gemini 利用不可 | Google AI Studio SDK に切替 |
| ES 認証 401 | Secret Manager に username/password 自動格納に変更 |
| モデル名 `gemini-1.5-flash` 廃止 | `gemini-2.5-flash` に変更 |
| eval イメージ未 push で tf-apply 失敗 | `images-push` 集約ターゲットで構造的に防止 |
| API 起動失敗（ES 未準備） | `depends_on = [module.elastic]` + アプリ側リトライ |
| `tf-destroy` が対話入力待ちで停止 | `-auto-approve` 追加 |
| BQ Vector Index 作成失敗（index_type 未指定） | `IVF` を指定 |
| BQ Vector Index 作成失敗（テーブル空） | ingestion-run の後に移動、5000行未満はスキップ |
| Gemini API レートリミットで評価中断 | リトライロジック追加（最大5回・指数バックオフ） |

---

## Phase 7: RAG評価基盤 --- 実装済み

RAGシステムの品質を定量化する評価基盤を構築。検索品質と回答品質の2層で評価する。

```
scripts/eval/
├── evaluate.py          # メイン評価スクリプト（--search-type / --save-as / --top-k）
├── metrics.py           # 指標計算（Recall@K / MRR / Exact Match / ROUGE-L）
├── report.py            # 横並び比較レポート生成
├── queries_eval.jsonl   # 評価クエリ20件 + Ground Truth
├── results/             # 評価結果 JSON 保存先
└── tests/
    ├── test_metrics.py  # metrics テスト
    └── test_report.py   # report テスト
```

### 評価指標

| 層 | 指標 | 説明 |
|---|------|------|
| 検索品質 | Recall@K (K=1,3,5,10) | 正解チャンクがTop-Kに含まれる割合 |
| 検索品質 | MRR | 最初の正解チャンクの順位の逆数の平均 |
| 回答品質 | Exact Match | 期待キーワードが全て含まれる割合 |
| 回答品質 | ROUGE-L | 文字レベルLCSベースの回答網羅性 |

### 検索パターン比較

`make eval-search-patterns` で vector / elasticsearch / hybrid の3パターンを比較し、ハイブリッド検索の効果を定量化する。

---

## Phase 8: Vertex AI Pipeline 統合 --- 実装済み

RAG評価基盤を Vertex AI Pipeline としてパッケージ化し、GCP 上で定期自動実行できるようにした。
Cloud Run Job 方式（直接実行）と Vertex AI Pipeline 方式（KFP オーケストレーション）が共存する。

### アーキテクチャ（デュアルパス）

```
=== パスA: Vertex AI Pipeline（KFP オーケストレーション）===

[CI/CD (GitHub Actions)]
  └── deploy-pipeline ジョブ
       ├── KFP JSON コンパイル
       ├── GCS にアップロード (gs://mlops-dev-a-doc-qa/pipeline-artifacts/)
       └── Vertex AI Pipeline スケジュール作成/更新（冪等）
             ↓
[Vertex AI Pipeline（毎週日曜 22:00 JST）]
  └── doc-qa-eval コンテナコンポーネント
       └── evaluate.py → 結果を GCS にアップロード

=== パスB: Cloud Run Job（直接実行・フォールバック）===

[Cloud Scheduler（毎週日曜 22:00 JST）]
  └── Cloud Run Job（doc-qa-eval）
       └── evaluate.py → 結果を GCS にアップロード
```

### 実装ファイル

```
src/pipeline/
├── pipeline.py              # KFP パイプライン定義（@dsl.pipeline）
├── run_pipeline.py          # compile / run / schedule コマンド（冪等スケジュール管理）
├── components/
│   └── run_evaluation.py    # コンテナコンポーネント（@dsl.container_component）
├── entrypoint.sh            # 環境変数設定 + evaluate.py 起動
├── Dockerfile               # 評価用イメージ（API モジュール + eval スクリプト同梱）
└── requirements.txt         # kfp>=2.7.0, google-cloud-aiplatform>=1.38.0
```

### Terraform リソース（compute モジュール）

| リソース | 説明 |
|---------|------|
| `google_cloud_run_v2_job.eval` | 評価用 Cloud Run Job（パスB） |
| `google_cloud_scheduler_job.eval_schedule` | Cloud Scheduler 週次トリガー（パスB） |
| ※ Vertex AI Pipeline スケジュール | CI/CD 管理（Terraform Provider 未対応のため） |

### CI/CD

`.github/workflows/doc-qa.yml` に3つのジョブ:

| ジョブ | 説明 |
|-------|------|
| `test` | ingestion / api のテスト実行 |
| `build-and-deploy` | 3コンポーネント（ingestion / api / pipeline）のビルド・プッシュ・デプロイ |
| `deploy-pipeline` | KFP コンパイル → GCS アップロード → Vertex AI Pipeline スケジュール作成/更新 |

### Makefile ターゲット

| コマンド | 説明 |
|---------|------|
| `make eval-pipeline-build` | 評価用 Docker イメージビルド |
| `make eval-pipeline-push` | イメージを Artifact Registry に push |
| `make eval-pipeline-compile` | パイプライン JSON コンパイル |
| `make eval-pipeline-run` | パイプラインをコンパイルして Vertex AI で実行 |
| `make eval-pipeline-schedule` | Vertex AI Pipeline 週次スケジュール作成/更新（冪等） |

### 技術選定: スケジュール管理

| 方式 | 状態 | 備考 |
|------|------|------|
| Terraform で管理 | 不可 | `google_vertex_ai_pipeline_schedule` リソースが Provider に存在しない |
| CI/CD で SDK 管理 | **採用** | `run_pipeline.py schedule` で冪等に作成/更新 |

---

## Phase 9: テスト拡充 --- 実装済み

テストが不足していた5モジュールに対し、計53テストを追加。

| テストファイル | 対象モジュール | テスト数 |
|---|---|---|
| `shared/tests/test_config.py` | config.py（load_config, get, setup_logging） | 10 |
| `scripts/eval/tests/test_metrics.py` | metrics.py（recall_at_k, mrr, exact_match, rouge_l 等） | 26 |
| `scripts/eval/tests/test_report.py` | report.py（generate_report, _get_metric） | 7 |
| `src/ingestion/tests/test_es_writer.py` | es_writer.py（ensure_index, write_chunks_to_es） | 5 |
| `src/api/tests/test_retriever.py` | retriever.py（search_bigquery_vector, search_elasticsearch, hybrid_search） | 5 |

### バグ修正

| 問題 | 修正 |
|------|------|
| `report.py` の `generate_report([])` で TypeError | 空リストの早期リターンを追加 |
| `compute/main.tf` の API Service が `var.gemini_api_key`（未定義）を参照 | `var.google_ai_studio_api_key` に修正 |

---

## Phase 10: React TypeScript フロントエンド --- 実装済み

FastAPI と同一ポート（8080）で React SPA を配信する構成。

### アーキテクチャ

```
FastAPI (Cloud Run, port 8080)
  ├── GET /              → static/index.html（SPA エントリ）
  ├── GET /assets/*      → React ビルド済み JS/CSS
  ├── GET /{path}        → SPA キャッチオール（React Router 対応）
  ├── POST /query        → JSON API（既存）
  ├── POST /ingest       → JSON API（既存）
  └── GET /health        → JSON API（既存）
```

### 画面構成

| パス | 画面 | 機能 |
|------|------|------|
| `/` | QA 検索 | テキスト入力 → `/query` API → 回答 + 根拠ドキュメント一覧 |
| `/upload` | ドキュメントアップロード | GCS パス指定 → `/ingest` API → Ingestion Job 実行 |
| `/eval` | 評価ダッシュボード | API ヘルスチェック + 評価結果表示 |

### 技術スタック

- **Vite** + **React 19** + **TypeScript** + **React Router**
- Dockerfile: multi-stage build（Node.js で React ビルド → Python イメージに COPY）
- 開発時: `npm run dev`（Vite dev server → FastAPI にプロキシ）

### ディレクトリ

```
src/frontend/
├── vite.config.ts                # ビルド設定（outDir: ../api/static）
├── src/
│   ├── App.tsx                   # React Router 定義
│   ├── types/api.ts              # API 型定義（Pydantic モデルと対応）
│   ├── pages/
│   │   ├── QueryPage.tsx         # QA 検索画面
│   │   ├── UploadPage.tsx        # アップロード画面
│   │   └── EvalPage.tsx          # 評価ダッシュボード
│   └── components/
│       ├── Layout.tsx            # ナビゲーション付き共通レイアウト
│       └── SourceCard.tsx        # 根拠ドキュメント表示カード
└── index.html
```

---

## 最終ディレクトリ構成

```
study-gcp-llm-rag-mlops-vertex/
├── .github/workflows/          # CI/CD
├── shared/                     # 共通ライブラリ（config.py + core.py + tests/）
├── src/
│   ├── ingestion/              # Cloud Run Job（extract/ embed/ store/ tests/）
│   ├── api/                    # Cloud Run Service（endpoints/ search/ generation/ static/ tests/）
│   ├── frontend/               # React TypeScript SPA（Vite）
│   └── pipeline/               # Vertex AI Pipeline（評価パイプライン）
├── scripts/                    # 運用スクリプト（eval/ monitor/ ops/ setup/）
├── terraform/modules/          # IaC（data/ compute/ elastic/ registry/ iam/）
├── env/config/                 # application.yml
├── env/secret/                 # credentials.yml（git管理外）
├── data/sample/                # サンプル社内規定
├── docs/                       # 仕様書・運用マニュアル
└── Makefile
```

---

## 今後の優先事項

今回の検証で `make deploy-all` が一発で通らず6回修正した。機能追加より足元の安定化を優先する。

### 優先度 高（安定化）

| 施策 | 理由 |
|------|------|
| ~~Gemini API 有料プラン化~~ | ~~無料枠 日次20回では評価も運用も回らない~~ → **対応済み** |
| ~~本番データ投入・BQ Vector Index 検証~~ | BQ Vector Index は IVF 方式で最低5000行必要かつ Embedding コストが大きいため**スキップ前提**とする。VECTOR_SEARCH は Index なしでも動作する |
| E2E テスト自動化 | `deploy-all` → ヘルスチェック → クエリ → `destroy-all` を CI で回す |
| ~~Elasticsearch kuromoji 有効化~~ | ~~standard アナライザーは妥協~~ → **対応済み**（Elastic Cloud に analysis-kuromoji プラグイン追加、ja_kuromoji カスタムアナライザー適用） |

### 優先度 中（機能拡張）

| 施策 | 理由 |
|------|------|
| ~~UI~~ | ~~仕様書 §14 Phase 4~~ → **対応済み**（FastAPI + React TypeScript SPA。1ポート統合配信。QA検索/Upload/Eval の3画面） |
| アクセス権限・Kibana ダッシュボード・Discord 連携 | 仕様書 §14 Phase 4 の残り |

### 優先度 低（将来検討）

| 施策 | 理由 |
|------|------|
| ~~`google.generativeai` → `google.genai` SDK 移行~~ | ~~非推奨警告が出ていた~~ → **対応済み**（`generator.py` + `requirements.txt` を `google-genai` に切替） |
| Vertex AI Gemini への切替 | コンソール同意が必要。現在の Google AI Studio で十分動作しており緊急性なし |
| ELECTRA 差し替え（ドメイン特化 Embedding） | 現在の text-multilingual-embedding-002 で Recall@3=1.0。精度改善の必要性が出てから |
